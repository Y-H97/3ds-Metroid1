# v0.1.0 — 2026-02-16

Kurz: Erstes veröffentlichbares Änderungsprotokoll für den aktuellen Entwicklungsstand.

## Hinzugefügt
- CI‑Jobs zur Validierung von Dokumentation (Obsidian‑Wiki‑Links) und Level‑Export.
- Pre‑commit Hook zur Prüfung auf deutsche Kommentare in Quell‑/Skriptdateien.
- VS Code Tasks für schnelle lokale Prüfungen (`Deutsche Kommentare`, `Wiki‑Links`, `LevelExport`).
- Skripte zur Automatisierung:
  - `scripts/check_german_comments.py` (+ PowerShell / .bat wrapper)
  - `scripts/check_wiki_links.py` (+ PowerShell / .bat wrapper)
- PR‑Template und repository‑weite AI/Copilot‑Richtlinien (`.github/*`, `docs/AI_REGELN.md`).

## Geändert
- Dokumentation: zentrale Hub‑Seite `docs/GESAMTDOKU.md` und `docs/workspace/EINSTEIGER_GUIDE.md` erweitert (Regeln, Tasks, How‑to).
- VS Code Workspace‑Settings / Tasks hinzugefügt (PowerShell als Default für Tasks).
- Level‑Dateien: deutsche Kopf‑Kommentare in mehreren `LevelEditor/level/*.lua` Dateien.

## Behoben
- Wrapper + Skriptrobustheit für UNC/CMD‑Probleme auf Windows (PowerShell + Batch‑Wrappers).

## Hinweise
- Regeln: Neuer/geänderter Code muss auf Deutsch kommentiert sein — CI/Pre‑commit überprüft dies.
- Nächste Schritte empfohlen: Branch‑Protection für CI‑Checks aktivieren, PR‑Vorlagen verfeinern.

---
`Changelog` erzeugt automatisch während der Dokumentations‑Aufräumarbeiten (siehe PR).